# Story 3.4 Edge Case Hunter Review

Review the current working tree changes against baseline commit `448bee1` in
`/Users/srudyka/slower/demo-bmad`, including untracked files. Invoke the
`bmad-review-edge-case-hunter` skill. Read the Story 3.4 file, project context,
and AWS Terraform implementation standard. Walk every policy, trusted-plan,
exception, workflow, manifest, and sanitized-evidence boundary. Return JSON
findings with `location`, `trigger_condition`, `guard_snippet`, and
`potential_consequence`. Do not modify files.

| Original Text | Revised Text | Changes |
|---------------|--------------|---------|
| Each AWS Account and Region receives one shared Platform Cell. | Each AWS Account-Region pair contains one shared Platform Cell. | Clarifies that a Cell belongs to each account-and-Region combination, not independently to every account and every Region. |
| Application repositories consume a per-job Terraform module that creates the ECS task definition, one EventBridge Scheduler launch schedule, job-scoped IAM roles, log group, alarms, and an immutable job configuration record. | Application repositories consume a per-job Terraform module that creates the ECS task definition, one EventBridge Scheduler launch schedule, job-scoped IAM roles, log group, alarms, and an immutable job configuration (`CONFIG`) record. | Defines `CONFIG` at first use for readers outside the implementation team. |
| Confirm retention defaults: 90-day production logs/occurrences, 30-day non-production logs, 35-day ledger PITR, and 14-day queues. | Confirm retention defaults: 90-day production logs and occurrences, 30-day non-production logs, 35-day ledger point-in-time recovery (PITR), and 14-day queues. | Expands the acronym at first use and replaces a compressed slash construction. |
| Confirm the pilot account, Region, jobs, owners, notification targets, operator controls, and measured Cell RPO/RTO before rollout. | Confirm the pilot account, Region, jobs, owners, notification targets, operator controls, and measured Cell recovery point and recovery time objectives (RPO/RTO) before rollout. | Expands unfamiliar operational acronyms at first use. |
| ECS events and structured logs complete the occurrence record; a transactional outbox preserves alerts. | ECS events and structured logs complete the occurrence record; a transactional outbox provides reliable alert delivery. | Replaces the vague phrase "preserves alerts" with the specific communication outcome. |
| Rejected: cannot materialize expectations independently or reliably process HTTP-200 `RunTask` responses containing `failures[]` | Rejected: does not independently materialize expectations or let the platform reliably process HTTP-200 `RunTask` responses containing `failures[]` | Supplies a clear subject and keeps the two rejection reasons grammatically parallel. |
| The shared Cell and its internal SQS redrive/Scheduler DLQs are required MVP platform internals; consumer-configurable payload DLQs remain deferred. The PRD and addendum must be reconciled to this adopted architecture before implementation stories are baselined. | The shared Cell and its internal SQS redrive and Scheduler dead-letter queues (DLQs) are required internal MVP components; consumer-configurable payload DLQs remain deferred. The PRD and addendum must be reconciled with this adopted architecture before implementation stories are baselined. | Expands `DLQ` at first use, removes compressed wording, and changes "reconciled to" to the idiomatic "reconciled with." |
| Application team via platform contract | Application team (through the platform contract) | Distinguishes the owner from the mechanism through which the team operates. |
| Perform work, remain idempotent/locked, emit occurrence-aware start/success/failure logs | Perform work, use idempotency or locking, and emit occurrence-aware start, success, and failure logs | Replaces ambiguous slash constructions and restores parallel verb structure. |
| Before launch, the Process Manager transactionally reserves attempt zero, the exact CONFIG version, a deterministic client token, first-request time, and a conservative retry deadline no later than one hour later. | Before launch, the Process Manager transactionally reserves attempt zero, the exact CONFIG version, a deterministic client token, the first-request time, and a conservative retry deadline within one hour of that request. | Removes the redundant "no later than ... later" construction and clarifies the deadline reference point. |
| ECS token TTL is the shorter of 24 hours or resource lifetime plus one hour, so no unresolved retry or replay calls `RunTask` after the conservative deadline. | The ECS token TTL is the shorter of 24 hours and one hour beyond the resource lifetime. Therefore, the system does not call `RunTask` for an unresolved retry or replay after the conservative deadline. | Fixes the comparative construction and removes an awkward inanimate subject ("retry or replay calls"). |
| Terraform publishes secret-free, content-addressed CONFIG to the job's exact prefix in a Cell-owned encrypted/versioned S3 inbox. | Terraform publishes secret-free, content-addressed CONFIG to the job's exact prefix in a Cell-owned, encrypted, versioned S3 inbox. | Replaces a slash with a parallel adjective series. |
| Checked-in transition fixtures reduce immutable deduplicated evidence identically for every bounded event permutation. | Checked-in transition fixtures verify that every bounded permutation of immutable, deduplicated evidence reduces to the same result. | Clarifies what the fixtures verify and adds the missing separator between coordinate adjectives. |
| IDs in application log text are assertions to compare, not authority. | IDs in application log text are assertions to validate, not authoritative identity. | Replaces an awkward verb phrase and makes the contrast explicit. |
| Process Manager poison message | Poison message received by the Process Manager | Clarifies that the Process Manager receives the poison message rather than producing or owning it. |
| All platform-created roles use a required permissions boundary. | A permissions boundary is mandatory for every platform-created role. | Removes the awkward combination of "use" and "required" while preserving the mandate. |
| Production sessions require approval and are attributable in CloudTrail; break-glass use is time-bound, alerted, independently approved, and reviewed. | Production sessions require approval and are attributable to a specific actor in CloudTrail; break-glass use is time-bound, generates an alert, requires independent approval, and is reviewed. | Clarifies attribution and replaces a compressed adjective series with parallel actions. |
| This places reusable-workflow identity inside the claim AWS can evaluate rather than relying on a separate unsupported custom claim. | This places reusable-workflow identity inside the `sub` claim that AWS can evaluate, rather than relying on a separate unsupported custom claim. | Names the claim explicitly and adds punctuation that separates the contrast. |
| Cell canary processed heartbeat. | Processed heartbeat from the Cell canary. | Restores natural modifier order. |
| Production acceptance injects every required failure and detects it within five minutes of the observable failure or deadline, with zero false alerts across at least 20 accelerated successful windows. | Production acceptance testing injects every required failure and must detect it within five minutes of the observable failure or deadline, with zero false alerts across at least 20 accelerated successful windows. | Gives the action a concrete subject and expresses the acceptance requirement directly. |
| Cell-wide failures page Platform on-call once. | Consider: Cell-wide failures page Platform on-call once per Cell incident. | Queries the unspecified scope of "once" rather than assuming the intended deduplication interval. |
| Before a job plan, the workflow reserves job ID to repository/root/apply identity, account, Region, and owner. | Before a job plan, the workflow binds the job ID to the repository, root, apply-role identity, account, Region, and owner. | Fixes the preposition, adds the missing article, and replaces the slash-separated identity list. |
| After phase one creates the IAM role and disabled schedule, the Registrar resolves and binds their immutable Role ID and schedule ARN. | After phase one creates the IAM role and disabled schedule, the Registrar resolves and binds the role's immutable Role ID and the schedule ARN. | Removes an unclear plural antecedent and maps each identifier to its resource. |
| Pull requests run `terraform fmt -check`, backend-free initialization/validation, module tests, examples, security scanning, IAM analysis, policy fixtures, and reviewed plan generation. | Pull requests run `terraform fmt -check`, initialization without backend access, validation, module tests, example checks, security scanning, IAM analysis, policy fixtures, and reviewed plan generation. | Replaces compressed slash wording and clarifies that examples are checked rather than merely present. |
| Provider lock files are committed and production initialization is read-only. | Provider lock files are committed, and production initialization verifies them without modification. | Clarifies what "read-only" applies to in this context. |
| CONFIG items, prior task definitions, Lambda versions, evidence, and logs remain available through the maximum compatibility/retry/runtime horizon. | CONFIG items, prior task definitions, Lambda versions, evidence, and logs remain available through the longest applicable compatibility, retry, or runtime horizon. | Clarifies how the three retention horizons combine. |
| Application data side effects require the Job Owner's tested compensating procedure before destructive cleanup. | Before destructive cleanup, the Job Owner must run the tested procedure for compensating application data side effects. | Makes the actor and required action explicit. |


| Original Text | Revised Text | Changes |
|---------------|--------------|---------|
| CloudWatch is the default logs, metrics, alarms, and dashboard platform. | CloudWatch is the default platform for logs, metrics, alarms, and dashboards. | Fixed awkward noun stacking and made the list parallel. |
| Production-impacting changes include expected impact and rollback notes. | Production-impacting changes document expected impact and rollback steps. | Removed the repeated use of “impact” as both modifier and object; made the required action explicit. |
| Consumers pin a semantic release version rather than a moving branch. An initial source shape is: | Consumers pin a semantic release version rather than a moving branch. A representative source declaration is: | Replaced ambiguous jargon with the established Terraform term for the example. |
| The architecture can use structured CloudWatch Logs parsing, DynamoDB-backed run tracking, Scheduler-supplied occurrence metadata, ECS task metadata plus a reporter, a lightweight completion API, CloudWatch Embedded Metric Format, or another mechanism that meets the occurrence-level contract. | The implementation may use structured CloudWatch Logs parsing, DynamoDB-backed run tracking, Scheduler-supplied occurrence metadata, ECS task metadata plus a reporter, a lightweight completion API, CloudWatch Embedded Metric Format, or another mechanism that meets the occurrence-level contract. | Replaced an unclear actor (“architecture”) with the system being implemented; preserved the provisional choice. |
| Scheduler target success concerns the target API request. | Scheduler target success indicates only the outcome of the target API request. | Clarified what the success signal represents and emphasized its limited scope. |
| Native Scheduler CloudWatch metrics are best-effort and grouped by Schedule Group rather than individual schedule. A reliable per-job missed-run promise requires a separate completion signal and deadline evaluation. | Native Scheduler CloudWatch metrics are best-effort and grouped by Schedule Group rather than by individual schedule. Reliable per-job missed-run detection requires a separate completion signal and deadline evaluation. | Fixed faulty comparison parallelism and replaced “promise” with the precise capability being discussed. |
| A Fargate `stopTimeout` is termination grace, not a maximum execution duration. | A Fargate `stopTimeout` defines a termination grace period, not a maximum execution duration. | Added the missing noun and clarified the setting’s function. |
| GitHub OIDC trust must restrict audience and subject to the intended repository and deployment context. | GitHub OIDC trust must restrict the audience and subject claims to the intended repository and deployment context. | Identified “audience” and “subject” as claims, removing grammatical ambiguity. |
| GitHub's OIDC subject formats are evolving toward immutable organization and repository IDs. Account onboarding must generate trust for the repository's active subject format instead of assuming a name-only subject. | GitHub's OIDC subject formats are evolving toward immutable organization and repository IDs. Account onboarding must generate a trust policy for the repository's active subject format instead of assuming a name-only subject. | Specified the artifact generated during onboarding. |
| Reusable workflows and third-party actions should be pinned to full commit SHAs. Terraform provider lock files do not pin remote module versions, so production consumers should use an immutable registry version or Git commit rather than relying on a movable Git tag. | Pin reusable workflows and third-party actions to full commit SHAs. Terraform provider lock files do not pin remote module versions. Production consumers should therefore use an immutable registry version or Git commit instead of a movable Git tag. | Used direct instruction and split a dense sentence so the remote-module consequence is easier to scan. |

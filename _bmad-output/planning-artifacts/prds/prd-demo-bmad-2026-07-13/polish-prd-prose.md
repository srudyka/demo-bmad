| Original Text | Revised Text | Changes |
|---------------|--------------|---------|
| It is more than a Terraform Module: documentation, review gates, release management, alarms, operational ownership, and rollback are part of the product. | The Platform Service is more than a Terraform Module: documentation, review gates, release management, alarms, operational ownership, and rollback are part of the product. | Replaced an unclear pronoun whose nearest antecedent was “product bet” (§1). |
| **SRE / On-call Engineer:** responds to alarms and diagnoses failures using deployment identity, logs, task events, and the Runbook. | **SRE / On-call Engineer:** responds to alarms and diagnoses failures using Deployment Identity, logs, task events, and the Runbook. | Matched the defined term's capitalization (§3.1). |
| The job contract declares the supported expression type, flexible-window behavior, time-zone and daylight-saving semantics, and a canonical expected occurrence plus completion deadline; flexible windows are disabled by default. | The job contract declares the supported expression type, flexible-window behavior, time-zone and daylight-saving semantics, and a canonical Expected Occurrence with a completion deadline; flexible windows are disabled by default. | Matched the defined term's capitalization and replaced the imprecise “plus” (§5.2, FR-6). |
| Version one treats maximum runtime as a completion deadline used for overdue detection and response, not as guaranteed forced termination; enforced cancellation is deferred. | Version one treats maximum runtime as a completion deadline used for overdue detection and response, not as a guarantee of forced termination; enforced cancellation is deferred. | Corrected an awkward noun phrase (§5.2, FR-8). |
| The Platform Service creates or configures separate schedule invocation, ECS task execution, and application task IAM Roles with only their required responsibility. | The Platform Service creates or configures separate schedule-invocation, ECS task-execution, and application-task IAM Roles with only the permissions required for each responsibility. | Fixed number agreement and clarified that permissions, not roles, are restricted (§5.3, FR-10). |
| Scheduler `ecs:RunTask` access is restricted to the intended task-definition family and ECS Cluster, and `iam:PassRole` includes the appropriate passed-to-service condition. | Scheduler `ecs:RunTask` access is restricted to the intended task-definition family and ECS Cluster, and `iam:PassRole` includes the appropriate `iam:PassedToService` condition. | Replaced a loose paraphrase with the exact IAM condition key (§5.3, FR-10). |
| An Application Engineer can reference approved secrets without placing secret values in Git, ordinary Terraform environment inputs, plan output, or job documentation. | An Application Engineer can reference approved secrets without placing secret values in Git, ordinary Terraform input variables, plan output, or job documentation. | Clarified that “environment inputs” means Terraform input variables rather than process environment variables (§5.3, FR-12). |
| The Platform Service launches production ECS Tasks without public IP assignment into consumer-supplied private subnets and minimally scoped security groups. | The Platform Service launches production ECS Tasks in consumer-supplied private subnets and minimally scoped security groups, with public IP assignment disabled. | Fixed a misplaced modifier that made “without public IP assignment into” difficult to parse (§5.3, FR-13). |
| The Job Owner documents recognizable start, successful completion, and failure log events without exposing secrets; every signal includes the Occurrence ID, and examples use a structured success marker such as `JOB_COMPLETED_SUCCESSFULLY job_name=example-job occurrence_id=2026-07-13T10:00:00Z`. | The Job Owner documents recognizable start, successful-completion, and failure log events without exposing secrets. Every signal includes the Occurrence ID; examples use a structured success marker such as `JOB_COMPLETED_SUCCESSFULLY job_name=example-job occurrence_id=2026-07-13T10:00:00Z`. | Split an overloaded sentence and clarified that “successful completion” modifies the log-event type (§5.4, FR-14). |
| A normal zero-exit completion does not create a failure alarm. | A normal completion with a zero exit code does not create a failure alarm. | Replaced compressed jargon with a directly readable phrase (§5.4, FR-16). |
| For every Expected Occurrence, the platform can report one of these states: expected, started, completed successfully, failed, overdue or missed, or duplicate or ambiguous completion. | For every Expected Occurrence, the platform can report one of these states: expected; started; completed successfully; failed; overdue or missed; or duplicate or ambiguous completion. | Used semicolons to make the grouped alternatives unambiguous (§5.4, FR-17). |
| An Application Engineer can supply the existing notification target for production failures; alerts include job, Occurrence ID where applicable, Environment, AWS Account, failure plane, occurrence state, detection time, and Runbook location. | An Application Engineer can supply the existing notification target for production failures; alerts include job, Occurrence ID where applicable, Environment, AWS Account, failure plane, occurrence state, detection time, and the Runbook location. | Added the missing article before the final list item (§5.4, FR-18). |
| Exceptions require an owner, justification, approver, expiry or review date, and audit trail. | Exceptions require an owner, a justification, an approver, an expiry or review date, and an audit trail. | Restored parallel construction in the evidence list (§5.5, FR-21). |
| The Terraform Module is published from an internal GitHub repository, reusable workflows are published from a centralized platform repository, consumers pin release versions, and upgrade notices use GitHub releases plus an internal engineering channel. | The Terraform Module is published from an internal GitHub repository, reusable workflows are published from a centralized platform repository, consumers pin immutable release references, and upgrade notices use GitHub releases plus an internal engineering channel. | Aligned “pin release versions” with the later requirement for immutable registry versions or commits (§5.5, FR-25, A8). |
| Failure-injection tests detect every required schedule-delivery, `RunTask` response, launch, runtime, and completion failure within five minutes of the observable failure or declared deadline, with zero false alerts across at least 20 accelerated successful schedule windows. | Failure-injection tests detect every required failure scenario across schedule delivery, `RunTask` response handling, launch, runtime, and completion within five minutes of the observable failure or declared deadline, with zero false alerts across at least 20 accelerated successful schedule windows. | Removed faulty noun stacking that made a `RunTask` response itself sound like a failure (§5.6, FR-28). |
| **NFR-2:** Secrets never appear in source control, normal Terraform variable values, unrestricted logs, PR comments, saved-plan exposure, or documentation. | **NFR-2:** Secrets never appear in source control, normal Terraform variable values, unrestricted logs, PR comments, saved plans exposed to unauthorized readers, or documentation. | Replaced the unclear phrase “saved-plan exposure” with the prohibited condition (§6.1). |
| **NFR-16:** Production plans, approvals, exceptions, applies, Deployment Identity, and rollback evidence are attributable to an actor and retained according to organizational policy. | **NFR-16:** Production plans, approvals, exceptions, apply operations, Deployment Identity, and rollback evidence are attributable to an actor and retained according to organizational policy. | Replaced “applies” used as a noun with the precise operational term (§6.4). |
| **SM-1: Basic job setup time** — Median elapsed engineering time from ready inputs to a deployable basic Scheduled Job is under four hours and at least 50% lower than the validated baseline. | **SM-1: Basic job setup time** — Median engineering effort from ready inputs to a deployable basic Scheduled Job is under four hours and at least 50% lower than the validated baseline. | Query: use this revision if the metric measures hands-on effort; otherwise replace “elapsed engineering time” with “elapsed time” to measure wall-clock lead time (§9.1). |
| **SM-8: Observability escape rate** — Track incidents or failed runs where missing logs, alarms, completion signals, or ownership delayed detection or diagnosis; the target is zero for pilot and post-pilot jobs. | **SM-8: Observability escape rate** — Track incidents or failed runs in which missing logs, alarms, completion signals, or ownership delayed detection or diagnosis; the target is zero for pilot and post-pilot jobs. | Replaced “where” with the correct relative phrase for events (§9.2). |
| **Non-production enforcement:** Terraform formatting and validation failures and obvious secret exposure are blocking from MVP start. [ASSUMPTION A11: Other dev and staging policy findings are advisory during MVP; after pilot acceptance, missing tags become blocking in non-production, and within one or two adoption sprints staging also blocks IAM wildcards, missing retention or alarms, and mutable images.] | **Non-production enforcement:** Terraform formatting and validation failures and obvious secret exposure are blocking from MVP start. [ASSUMPTION A11: Other dev and staging policy findings are advisory during MVP; after pilot acceptance, missing tags become blocking in non-production, and within one or two adoption sprints, staging also blocks IAM wildcards, missing retention or alarms, and mutable images.] | Added the comma after an introductory prepositional phrase (§10, step 5). |
| For schedule-only failure, disable or restore the schedule independently of deleting logs or task definitions. | For schedule-only failures, disable or restore the schedule independently of deleting logs or task definitions. | Corrected the count noun (§10, Rollback Principles). |
| Module abstraction hides important behavior | Module abstraction hides important behavior | Preserve as written; the accompanying impact should change from “Unsafe adoption and hard diagnosis” to “Unsafe adoption and difficult diagnosis.” “Hard diagnosis” is an unnatural construction (§11). |
| Which one or two low-risk jobs will pilot, and which named Job Owners accept operational responsibility? | Which one or two low-risk jobs will be used in the pilot, and which named Job Owners will accept operational responsibility? | Removed the incorrect use of “pilot” as an intransitive verb and restored parallel verb phrases (§12, OQ-1). |
| One or two low-risk internal jobs pilot within one sprint; Platform Engineering / DevOps owns candidates until named Job Owners are assigned. | One or two low-risk internal jobs enter the pilot within one sprint; Platform Engineering / DevOps owns the pilot candidates until named Job Owners are assigned. | Applied the same “pilot” correction and clarified what “candidates” refers to (§13, A7). |
